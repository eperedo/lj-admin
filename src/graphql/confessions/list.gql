query confessions($limit: Int, $offset: Int) {
	confessions {
		author {
      id
      name
      nickname
    }
    id
    involved {
      id
      name
      nickname
    }
    links
    longText
    money
    shortText
    when
	}
}